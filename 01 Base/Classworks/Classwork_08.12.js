//1
// let text = prompt("Enter your text:");
// let text2 = "huba sajdf sd d gdg er d r rd ";

// let space = (text) => console.log(text.split(" ").length -1);

// //2
// let bigger = (text2) => console.log(text2[0].toUpperCase());

// //3
// let words = (text) => console.log(text.split(" ").length);

//4
const startWord = 'cascading style sheets';
function makeAbbr(startWords) {
    let resultWord = '';
  
    for (const char of words) {
      if (words.charAt(char)) {
        resultWord += char;
      }
    };
  
    return resultWord;
  }

